import { Button } from "@/components/ui/button";

const Footer = () => {
  return (
    <footer className="bg-card border-t border-border">
      <div className="container mx-auto px-4 py-8">
        {/* Simple footer without social icons */}
      </div>
    </footer>
  );
};

export default Footer;